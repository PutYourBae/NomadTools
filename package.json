{
  "name": "nomad-tools",
  "version": "1.0.0",
  "description": "NomadTools — FiveM Cache Switcher & Server Monitor",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
